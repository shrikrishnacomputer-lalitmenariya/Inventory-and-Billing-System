const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const bill = await prisma.bill.findFirst({ where: { billNumber: { contains: '339' } }, include: { billItems: { include: { product: { include: { category: true } } } } } }); console.log(JSON.stringify(bill, null, 2)); } main().catch(console.error).finally(() => prisma.());
